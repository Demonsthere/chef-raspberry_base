[Unit]
Description=qBittorrent Daemon Service
After=network.target

[Service]
Type=forking
User=<%= node[:raspberry_base][:user] %>
ExecStart=/usr/bin/qbittorrent-nox -d

[Install]
WantedBy=multi-user.target
